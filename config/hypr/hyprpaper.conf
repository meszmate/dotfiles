# hyprpaper — current block syntax (the old preload/wallpaper lines
# stopped working). Empty monitor = fallback for all monitors.

splash = false

wallpaper {
    monitor =
    path = ~/.config/hypr/wallpaper.jpg
    fit_mode = cover
}
